const expect = require('chai').expect;
const assert = require('chai').assert;
/*
import { EmployeeFactory, Employee } from '../src/creational/factory-method/factory-method_es6';

describe('factory method test', () => {

    it('EmployeeFactory Test', () => {
        const factory = new EmployeeFactory();
        expect(factory.getNumberOfEmployees()).to.equal(0);
        let employee1 = factory.findOrCreateEmployee("A");
        assert.instanceOf(employee1, Employee, 'Instance of Employee');
        expect(factory.getNumberOfEmployees()).to.equal(1);
        let employee2 = factory.findOrCreateEmployee("A");
        expect(factory.getNumberOfEmployees()).to.equal(1);
        expect(employee2 === employee1).to.be.true;
        let employee3 = factory.findOrCreateEmployee("B");
        expect(factory.getNumberOfEmployees()).to.equal(2);
        expect(employee3 === employee1).to.be.false;

    });


});
*/
